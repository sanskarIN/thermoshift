name: Desktop Platform Verification

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-platforms-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            artifact: linux
          - os: windows-latest
            artifact: windows
          - os: macos-14
            artifact: macos
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Linux native prerequisites
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack
      - name: Require exact release inputs
        run: npm run check:release-inputs
      - name: Install locked JavaScript dependencies
        run: npm ci --ignore-scripts
      - name: Verify repository metadata
        run: npm run check:versions
      - name: Verify desktop frontend configuration
        run: npm run check:desktop-config
      - name: Verify internal documentation links
        run: npm run check:docs
      - name: Check desktop Rust crate
        run: cargo check --locked -p thermoshift-desktop
      - name: Build unsigned native package
        run: npm run desktop:build
      - name: Write candidate metadata
        shell: bash
        run: |
          mkdir -p apps/desktop/src-tauri/target/release/bundle
          printf 'sha=%s\nref=%s\nos=%s\n' "$GITHUB_SHA" "$GITHUB_REF_NAME" "${{ matrix.artifact }}" > apps/desktop/src-tauri/target/release/bundle/THERMOSHIFT_BUILD.txt
      - name: Upload native bundle evidence
        uses: actions/upload-artifact@v4
        with:
          name: thermoshift-${{ matrix.artifact }}-bundle-${{ github.sha }}
          path: apps/desktop/src-tauri/target/release/bundle/**
          if-no-files-found: error
          retention-days: 14
