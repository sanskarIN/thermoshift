{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the ThermoShift main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
